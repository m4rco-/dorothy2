#!/usr/bin/env ruby

#libdir = File.dirname(__FILE__) + "./lib"
#libdir = File.expand_path(libdir)
#$: << libdir

require 'rubygems'
require 'trollop'
require 'dorothy2'

include DoroEnv
include Dorothy


#VERBOSE = (opts[:verbose] ? true : false)
#daemon = (opts[:daemon] ? true : false)

#Logging
LOGGER = DoroLogger.new(DoroEnv::LOGFILE, DoroEnv::LOGAGE)
LOGGER.sev_threshold = DoroEnv::LOGLEVEL

Dorothy.stop


